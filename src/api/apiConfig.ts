// For a local backend, it might be something like 'http://localhost:3000/api' or 'http://192.168.X.X:3000/api'
// IMPORTANT: For Android emulators, 'localhost' might point to the emulator itself (use 10.0.2.2 instead).
// Use your machine's network IP address if testing on a physical device.

// Ensure this matches the IP you are successfully using for authService
export const API_BASE_URL = 'http://192.168.255.201:3000/api'; // Example, use your actual working IP 